#pragma once

// We keep minimal C++ string literals here as fallbacks.
// Real signatures will be loaded from signatures.json at runtime.
// The strings below are placeholders and should NOT be treated as final offsets.
inline const char* FALLBACK_SIG_PLAYER_POS = "\xF3\x0F\x10\x89\x00\x00\x00\x00";
inline const char* FALLBACK_MASK_PLAYER_POS = "xxxx????";
