#pragma once

// fallback signatures only — used if JSON fails to load
inline const char* FALLBACK_SIG_PLAYER_POS =
    "\xF3\x0F\x10\x89\x00\x00\x00\x00";  // placeholder only

inline const char* FALLBACK_MASK_PLAYER_POS =
    "xxxx????";
